{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Read-mostly: no fs, shell, or network plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
